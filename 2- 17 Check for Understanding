def targetScore(hit):
    hit = int(input("What level did you hit?"))
    if hit == 1:
        return 50
    if hit == 2:
        return 40
    if hit == 3:
        return 30
    if hit == 4:
        return 20
    if hit == 5:
        return 10
    
print("Your score is:", targetScore(1))
    
